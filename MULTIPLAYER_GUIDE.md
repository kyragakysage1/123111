# 👥 Режим мультиплеер - Руководство по реализации

## Описание

Режим мультиплеер позволяет двум игрокам одновременно участвовать в игре с одинаковыми вопросами. Один игрок является "хозяином" (отправляет приглашение), другой - "гостем" (принимает приглашение).

## Как это работает

### 1. **Отправка приглашения**
- Игрок переходит в Мультиплеер
- Выбирает количество вопросов (5-50)
- Выбирает друга из списка
- Клик на "Пригласить"
- Приглашение отправляется в БД

### 2. **Получение уведомления**
- Друг видит всплывающее уведомление в правом углу экрана
- Может принять или отклонить приглашение
- Уведомление исчезает через 30 секунд автоматически

### 3. **Принятие приглашения**
- Клик "Принять" на уведомлении
- Экран переходит в режим игры
- Оба игрока видят одни и те же вопросы в одном порядке

### 4. **Игровой процесс**
- Каждому игроку по 30 секунд на ответ
- Вопросы одинаковые для обоих
- Можно видеть счёт противника в реальном времени
- После завершения всех вопросов показывается результат

### 5. **Завершение игры**
- Показывается экран с результатами
- Сравнение счёта между игроками
- Статистика и процент правильных ответов

## Структура файлов

### 1. **`js/gameModes/multiplayerMode.js`**
Основной класс `MultiplayerMode` с методами:
- `startAsHost()` - запуск игры как хозяина
- `startAsGuest()` - запуск игры как гостя
- `createGameSession()` - создание сессии в БД
- `submitAnswer()` - отправка ответа
- `updatePlayerScore()` - обновление счёта
- `endGame()` - завершение игры

### 2. **`js/auth/friendsManager.js`**
Добавлены методы для мультиплеера:
- `sendMultiplayerInvite()` - отправить приглашение
- `acceptMultiplayerInvite()` - принять приглашение
- `rejectMultiplayerInvite()` - отклонить приглашение
- `getIncomingInvites()` - получить входящие приглашения
- `setupMultiplayerInviteListener()` - слушатель в реальном времени
- `showMultiplayerInviteNotification()` - показать уведомление

### 3. **`js/script.js`**
Добавлены функции:
- `showMultiplayerScreen()` - показать экран выбора друзей
- `inviteFriendToMultiplayer()` - пригласить друга

### 4. **`index.html`**
- Новый экран `multiplayer-screen` с выбором друзей
- Настройка количества вопросов
- Кнопка для приглашения

### 5. **`DATABASE_SETUP.sql`**
Новые таблицы:
- `multiplayer_invites` - приглашения на игру
- `multiplayer_sessions` - сессии игр
- RLS политики для безопасности

## Требуемые таблицы БД

### Таблица `multiplayer_invites`
```sql
id (UUID) - первичный ключ
from_user_id (UUID) - кто отправил приглашение
to_user_id (UUID) - кому отправлено приглашение
status (VARCHAR) - pending, accepted, rejected
max_questions (INTEGER) - количество вопросов
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
```

### Таблица `multiplayer_sessions`
```sql
id (UUID) - первичный ключ
host_id (UUID) - хозяин игры
guest_id (UUID) - гость
max_questions (INTEGER) - количество вопросов
host_score (INTEGER) - счёт хозяина
guest_score (INTEGER) - счёт гостя
status (VARCHAR) - waiting_acceptance, in_progress, completed
started_at (TIMESTAMP)
ended_at (TIMESTAMP)
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
```

## Установка

### 1. Создать таблицы в Supabase
1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте содержимое `DATABASE_SETUP.sql`
4. Выполните SQL скрипт

### 2. Подключить скрипты
Все скрипты уже подключены в `index.html`:
```html
<script src="js/gameModes/multiplayerMode.js"></script>
```

### 3. Инициализация
`friendsManager` автоматически инициализирует слушатель приглашений при инициализации:
```javascript
window.friendsManager.setupMultiplayerInviteListener();
```

## Использование

### Для отправки приглашения:
```javascript
// Автоматически вызывается при клике на "Пригласить"
await window.friendsManager.sendMultiplayerInvite(friendId, maxQuestions);
```

### Для принятия приглашения:
```javascript
// Автоматически вызывается при клике "Принять" на уведомлении
await window.friendsManager.acceptMultiplayerInvite(inviteId);
```

### Для отклонения приглашения:
```javascript
// Автоматически вызывается при клике "Отклонить" на уведомлении
await window.friendsManager.rejectMultiplayerInvite(inviteId);
```

## Особенности

✅ **Синхронизированные вопросы** - оба игрока видят одни и те же вопросы в одном порядке

✅ **Реальное время** - счёт противника обновляется в реальном времени через Supabase

✅ **Уведомления** - всплывающие уведомления о приглашениях прямо на главном экране

✅ **Таймер** - 30 секунд на каждый вопрос

✅ **Результаты** - подробная статистика после игры

✅ **Безопасность** - RLS политики в БД для защиты данных

## Возможные улучшения

- 🎮 Поддержка более чем 2 игроков
- 🏆 Система достижений за мультиплеер
- 💾 История игр мультиплеера
- ⚡ Специальные бонусы за серии правильных ответов
- 🎵 Разные категории вопросов
- 📊 Рейтинг мультиплеера

## Решение проблем

### Приглашение не приходит
- Проверьте подключение к интернету
- Убедитесь, что оба пользователя авторизованы
- Проверьте, что в `multiplayer_invites` таблица создана

### Уведомления не появляются
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что `setupMultiplayerInviteListener` вызывается
- Проверьте, что `friendsManager` инициализирован

### Игра не синхронизируется
- Проверьте, что оба игрока видят одинаковые вопросы
- Проверьте, что `multiplayer_sessions` таблица создана
- Проверьте, что счёт обновляется в БД

## Тестирование

1. Создайте 2 аккаунта
2. Добавьте друг друга в друзья
3. С аккаунта A отправьте приглашение на мультиплеер
4. На аккаунте B примите приглашение
5. Оба должны начать играть с одинаковыми вопросами
6. После завершения проверьте результаты

---

✨ Мультиплеер готов к использованию!
